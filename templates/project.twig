<!DOCTYPE html>
<html>
    {% include "head.twig" %}
    <body>
        <div class="default-background">
            {% include 'nav.twig' %}

            <p style="margin: 20px">
                <a href="/{% if token %}?token={{ token }}{% endif %}">← terug naar het register</a>
            </p>

            <header class="default-color content-header">
                <h1>Projectinformatie</h1>
            </header>

            <div style="background: #F4FAFF">
                <a class="button" style="background: #4D80E2; float: right; margin: 1em" href="">bewerken</a>
                <b>Naam</b>: {{ title }}<br>
                <b>Afdeling</b>: {{ department }}<br>
                <b>Contact</b>: {{ contact_name }} (<a href="mailto:{{ contact_email }}">{{ contact_email }}</a>)<br>
                <b>Openbaar</b>: ja<br>
                <b>json</b>: <a href="{{ uri }}" target="_blank">.json data</a>
            </div>

            <div>
                <p>
                    <input type="checkbox" checked> <b>In dit project worden algoritmes ingezet</b>
                </p>
                <p>Bij inzet van algoritmes is <a href="/details/{{ id }}{% if token %}?token={{ token }}{% endif %}">Algoritmeregistratie</a> verplicht</p>
                <p>Bij inzet van algoritmes wordt de <a href="">Impact Assessment Mensenrechten en Algoritmes</a> aanbevolen</p>
            </div>
        </div>
        {% include "footer.twig" %}
    </body>
</html>